import React from 'react';
import classes from './SimpleLoader.module.css';

const SimpleLoader = (): JSX.Element => {
  return <span className={classes.SimpleLoader}></span>;
};

export default SimpleLoader;
